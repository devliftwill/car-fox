# Car Fox daemon — production deps (mirrors the working local .venv, Python 3.12).
# pipecat-ai[lemonslice] pulls in [daily]; [google] pulls google-genai for Gemini Live.
pipecat-ai[google,lemonslice]==1.5.0
aiohttp==3.14.1
loguru==0.7.3
python-dotenv==1.2.2
